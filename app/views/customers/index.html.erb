<p id="notice"><%= notice %></p>

<div class="card">

<br>

<h5>Customer List</h5>

<br>
<br>

<div class="table-responsive border-bottom" data-toggle="lists" data-lists-values='["js-lists-values-customer-name"]'>
  
  <%# <div class="search-form search-form--light m-3">
    <input type="text" class="form-control search" placeholder="Search">
    <button class="btn" type="button" role="button"><i class="material-icons">search</i></button>
  </div>
   %>
  <table class="table mb-0 thead-border-top-0">
    <thead>
      <tr>
        
        
        


      <th>Customer Name</th>
      <th>Contact No</th>
      <th>Contact Name</th>
      <th>Address</th>
      <th>City</th>
      <th>Date</th>
      <th>Sale Person Code</th>
      <th>Sale Person Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody class="list" id="blah">
    <% @customers.each do |customer| %>
      <tr>
        <td><%= customer.custoner_name %></td>
        <td><%= customer.contactno %></td>
        <td><%= customer.contact_name %></td>
        <td><%= customer.address %></td>
        <td><%= customer.city %></td>
        <td><%= customer.date %></td>
        <td><%= customer.sale_person_code %></td>
        <td><%= customer.sale_person_name %></td>
        <td><%= link_to 'Show', customer %></td>
        <td><%= link_to 'Edit', edit_customer_path(customer) %></td>
        <td><%= link_to 'Destroy', customer, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

</div>
</div>

<br>

<%= link_to 'New Customer', new_customer_path %>





